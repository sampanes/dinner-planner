# Clients

This folder is reserved for product surfaces that consume shared project data.

- `alexa/` is the future Alexa skill package, Lambda code, and APL documents.

The current static web site still lives at the repo root so the existing Pages-style deployment keeps working during phase 0.
